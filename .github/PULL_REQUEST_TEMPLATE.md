## What & why

## Changes

## Checklist
- [ ] `npm test` passes
- [ ] `npm run build` run and `dist/testctl.cjs` committed (if `lib/` or `bin/` changed)
- [ ] CHANGELOG + version bumped (if user-facing)
- [ ] No secrets / personal paths in the diff
